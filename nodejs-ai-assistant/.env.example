# Server port
PORT=4550

# Stream Chat server credentials
STREAM_API_KEY=
STREAM_API_SECRET=
STREAM_APP_ID=

# MedGemma (Gradio) configuration
GRADIO_BASE_URL=
GRADIO_API_NAME=/predict

# Default agent platform (only medgemma supported)
DEFAULT_AGENT_PLATFORM=medgemma
